<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>4.1.0</version>
    <relativePath/>
  </parent>

  <groupId>dev.leilaalgarve.jogoacoes</groupId>
  <artifactId>blackbox-proxy</artifactId>
  <version>0.0.1-SNAPSHOT</version>
  <name>blackbox-proxy</name>
  <description>
    Spec 05-020 -- reverse proxy, blackbox-only test tooling, never deployed to
    staging/production: sits in front of `app` and overrides Sec-CH-UA*/User-Agent on every
    forwarded request, letting a browser (Swagger UI included) test device-label resolution
    (spec 05-009) it otherwise can't reach -- the Fetch Standard forbids page scripts from
    setting any Sec-* header themselves.
  </description>

  <properties>
    <java.version>21</java.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>

</project>
